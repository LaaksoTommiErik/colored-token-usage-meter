{
  "name": "colored-token-usage-meter",
  "version": "0.1.0",
  "private": true,
  "description": "Codex CLI/OpenClaw colored token usage meter hooks",
  "scripts": {
    "test": "node --test tests/*.test.mjs",
    "check": "tests/run-tests.sh"
  }
}
